# Alertmanager：告警的路由、分组、抑制
#
# 虽然没有 kube-prometheus-stack 那么全，但这套配置覆盖了生产最核心的三件事：
#   1. 按 severity 分流（严重的叫得又快又勤）
#   2. inhibit_rules 抑制（大问题出现时，同名小告警不再刷屏）
#   3. receiver 可插拔（学习期空着，生产接钉钉/企微/PagerDuty）

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: alertmanager-config
  namespace: monitoring
data:
  alertmanager.yml: |
    global:
      resolve_timeout: 5m

    route:
      receiver: default
      group_by: ['alertname', 'severity']
      group_wait: 30s
      group_interval: 5m
      repeat_interval: 3h
      routes:
        # critical 走更短的等待、更频繁的重发
        - matchers:
            - severity="critical"
          receiver: default
          group_wait: 10s
          repeat_interval: 1h

    receivers:
      - name: default
        # 告警富化服务：每条告警推给它，自动查指标+日志生成诊断卡片
        webhook_configs:
          - url: http://alert-enricher.monitoring.svc.cluster.local:8080/webhook
            send_resolved: true
      # 想再接钉钉，加第二个 webhook_config 指向钉钉机器人即可

    # critical 抑制同名 warning，避免同一个问题刷两条
    inhibit_rules:
      - source_matchers: ['severity="critical"']
        target_matchers: ['severity="warning"']
        equal: ['alertname']

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: alertmanager
  namespace: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alertmanager
  template:
    metadata:
      labels:
        app: alertmanager
    spec:
      containers:
        - name: alertmanager
          image: prom/alertmanager:v0.27.0
          args:
            - --config.file=/etc/alertmanager/alertmanager.yml
            - --storage.path=/alertmanager
            # 支持 POST /-/reload，ConfigMap 更新后热加载，不必重启 Pod
            - --web.enable-lifecycle
          ports:
            - containerPort: 9093
          resources:
            requests:
              memory: 64Mi
              cpu: 50m
            limits:
              memory: 256Mi
              cpu: 200m
          volumeMounts:
            - name: config
              mountPath: /etc/alertmanager
            - name: data
              mountPath: /alertmanager
      volumes:
        - name: config
          configMap:
            name: alertmanager-config
        - name: data
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: alertmanager
  namespace: monitoring
spec:
  type: NodePort
  selector:
    app: alertmanager
  ports:
    - port: 9093
      targetPort: 9093
      nodePort: 30093
